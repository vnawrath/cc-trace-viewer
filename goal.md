We need to show a cost of each request, and the total cost of each session.
Use the WebSearch tool to understand how a cost is calculated based on the token counts and the model being used. Find the current pricing for the models as well.
